@model FoodTrackerMVC.Models.ShoppingList

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ShoppingList</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.IdListHistory)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.IdListHistory)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.IdProduct)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.IdProduct)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="IdShoppingList" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
